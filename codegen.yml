overwrite: true
schema: "./src/graphql/schema/schema.graphql"
documents: null
generates:
  ./src/definitions/generated/graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
